app-id: io.github.dlippok.photometric-viewer
runtime: org.gnome.Platform
runtime-version: "44"
sdk: org.gnome.Sdk
command: photometric-viewer
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri

modules:
  - name: photometric-viewer
    buildsystem: simple
    build-commands:
      - pip3 install . --prefix ${FLATPAK_DEST}
    sources:
      - type: git
        url: "https://github.com/dlippok/photometric-viewer.git"
        tag: v1.0.0
        commit: bd500ea6353aec89b26a79710d58620bc818d094
